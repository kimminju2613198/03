#include <stdio.h>

int main(void)
{
    float a, b;

    printf("enter the numerator : ");
    scanf("%f", &a);

    printf("Enter the denominator : ");
    scanf("%f", &b);

    printf("division is %f \n", a / b);

    return 0;
}